{
  "name": "api-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "npx wrangler deploy",
    "typecheck": "npx tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250713.0",
    "typescript": "^5.7.0"
  }
}
